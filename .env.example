# ============================================================
# 안녕메디컬 정산 — 환경 변수
# ============================================================
# 현재 버전은 Supabase 없이 브라우저 localStorage 기반 로컬 데이터로 동작합니다.
# Vercel에 NEXT_PUBLIC_SUPABASE_* 값을 넣지 않아도 됩니다.

# Claude Vision API — OCR 실제 사용 시에만 설정
# 비워두면 /api/ocr 은 데모용 mock 응답을 반환합니다.
ANTHROPIC_API_KEY=sk-ant-...
