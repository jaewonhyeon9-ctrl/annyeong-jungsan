# 안녕정산 작업 일지 — 2026-05-20

## 오늘 한 일

### 1. 푸시 안 된 14개 파일 확인
- `fd3bbdd Phase 1: 마스터 데이터 admin 페이지` 까지는 푸시되어 있었음
- 작업 중이던 14개 파일이 커밋 안 된 상태로 남아있었음

### 2. Codex CLI 코드 리뷰
- `codex review --uncommitted -c sandbox_mode='"danger-full-access"'`
- (Windows 샌드박스 버그 우회를 위해 sandbox_mode 오버라이드 필요)
- **발견사항 1건 (P2)**:
  - `src/app/owner/patients/[id]/visit/page.tsx:153-155`
  - 편집 모드에서 메모/사진을 비워도 DB 에 반영되지 않음
  - 원인: `memo || undefined` / `photo ?? undefined` 로 보냄 → `visits.update` 는 `undefined` 를 "변경 안 함" 으로 해석

### 3. 수정 적용 (3개 파일)

**`src/lib/data/source.ts`**
```ts
// VisitCreateInput
visitMemo?: string | null;        // 기존: string
beforePhotoUrl?: string | null;
afterPhotoUrl?: string | null;
```

**`src/app/owner/patients/[id]/visit/page.tsx`** (편집 모드 분기)
```ts
// null 을 명시적으로 보내야 DB 의 기존 값을 지울 수 있다.
// undefined 는 update 에서 "변경 안 함" 으로 해석된다.
visitMemo: memo.trim() ? memo : null,
beforePhotoUrl: beforePhoto,
afterPhotoUrl: afterPhoto,
```

**`src/lib/data/mock.ts`** (Visit 타입 일관성 유지)
- create: `input.visitMemo ?? undefined`
- update: `patch.visitMemo !== undefined ? (patch.visitMemo ?? undefined) : current.visitMemo`

### 4. 검증
- **타입체크**: 제 수정으로 인한 새 에러 0건 (admin 페이지의 기존 에러는 별도 이슈)
- **Dev 서버 기동**: `http://localhost:3000` 200 OK
- **로직 단위 테스트**: `scripts/test-visit-update-fix.mjs` 5/5 PASS
  - 메모 비우기 (사진 유지) ✅
  - Before 사진 제거 (메모 유지) ✅
  - 전부 클리어 ✅
  - 공백만 입력된 메모 → clear 처리 ✅
  - 메모 수정 (실제 값 유지) ✅

### 5. 커밋 & 푸시
- 커밋: `8000d61 Phase 2: 방문 차트 편집/데이터 레이어 보완 + 가입 흐름 정리`
- 푸시: `fd3bbdd..8000d61 main -> main` ✅
- 14 files changed, 538 insertions(+), 116 deletions(-)

---

## 다음 작업 (내일 이어서)

### A. 도메인 구매 타이밍 (대화 마무리 못한 주제)
판단 기준:
- **브랜드명 확정됐고 마케팅 곧 시작** → **즉시 구매** (선점 리스크 큼)
- **이름 후보 단계** → 1주일 안에 확정 후 구매
- **MVP 검증 중** → 검증 끝낸 직후 (3~6개월)
- **이미 광고/SNS 운영 중** → 무조건 지금
- 한국 사업이면 `.com` + `.co.kr` + `.kr` 동시 확보 권장 (연간 3~5만원)
- 가비아 / 후이즈 / Cloudflare Registrar (가장 저렴, 마진 0)

### B. admin 페이지 타입 에러 정리
- `database.types.ts` 에 admin 테이블 (homecare/products/services/symptoms) 정의 누락
- `npx tsc --noEmit` 시 12개 에러 발생 중

### C. 실 브라우저에서 visit 편집 메모/사진 클리어 동작 최종 확인
- 단위 테스트는 통과했지만 실제 Supabase 환경에서도 확인 필요
- 환자 차트 편집 → 메모 비우고 저장 → 페이지 새로고침 → 빈 상태 유지되는지

---

## 환경 메모
- Codex CLI v0.132.0 — Windows 샌드박스 깨져있음. 사용 시 `-c sandbox_mode='"danger-full-access"'` 옵션 필요
- Claude Code v2.1.145
- Next.js 15.5.18 (Turbopack)
- Cursor 세팅은 아직 진행 안 함
